# Environment variables for MCP Gateway

# Logging level
LOG_LEVEL=INFO

# Gateway configuration
GATEWAY_PORT=8000
GATEWAY_HOST=0.0.0.0

# Default idle timeout for servers (seconds)
DEFAULT_IDLE_TIMEOUT=300

# Docker network (optional)
DOCKER_NETWORK=mcp-network

# Context7 configuration (if needed)
# CONTEXT7_API_KEY=your_api_key_here

# Other MCP server environment variables can be added here